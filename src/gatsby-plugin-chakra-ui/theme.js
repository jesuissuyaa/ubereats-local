import { theme as defaultTheme } from "@chakra-ui/core"

const theme = {
  ...defaultTheme,
  colors: {
    ...defaultTheme.colors,
    brandPurple: "#673FB4",
    brandYellow: "#FDC02F",
    brandGreen: "#8CC152",
  },
}

export default theme
